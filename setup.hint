sdesc: "Wherever Change Directory with wildcards."
ldesc: "Wcd manages cache of directories and user can jump to any of
the cache (sub)directories.-tree. The directory searches can be
expressed with partial names or with wildcards * ? [SET] and [^SET]."
category: Utils
requires: cygwin 

